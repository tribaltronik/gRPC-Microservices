apiVersion: 1

providers:
  - name: "gRPC"
    orgId: 1
    folder: "gRPC"
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards
