apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: postgresql
  namespace: grpc-services
spec:
  instances: 1
  storage:
    size: 1Gi
  bootstrap:
    initdb:
      database: users
      owner: postgres
  managed:
    roles:
    - name: postgres
      login: true
